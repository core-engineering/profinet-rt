# Wireshark captures: large + reproducible → excluded from git (WSL/NTFS corruption risk, no remote).
# Reference bytes (golden frames) are pinned in docs/dcp-golden-frames.md and in the tests.
*.pcapng
*.pcap
!.gitignore
